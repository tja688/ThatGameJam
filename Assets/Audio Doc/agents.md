# agents.md — Audio Integration (Audio Manager Pro) 落地执行书

> 目标：把项目的“音效/音乐打点文档”真正落到代码里，并提供一套可一键配置 + 可视化绑定资源 + 可调参的音频编辑器中枢。
> 插件：Audio Manager Pro（已导入项目；你需要先通读其源码/用法，形成“最小可用集成层”）

---

## 0. 你的身份与工作方式（AI 必读）

你是“偏工程的技术音频集成工程师 + Unity Editor 工具开发者”。
你需要：
1) **先理解**：通读 Audio Manager Pro 源码与示例（Assets/Plugins/QFSW/Audio Manager Pro），明确它的核心对象/入口 API/播放句柄/池化/混音器支持/2D-3D 选项/循环播放停止方式/持久化方式。
2) **再落地**：不要一上来就全项目乱改；遵循“先搭中枢、再接点位、再做编辑器”的顺序。
3) **最后验收**：必须能在 Test Scene / 正式关卡中验证：点位触发能播、空资源不报错、一键配置可复用。

---

## 1. 已知项目结构与打点文档（上下文）

### 1.1 项目 Feature 目录（用于你定位改点位）
脚本大致在 `Assets/Scripts/Features/*` 下按系统分文件夹（Player、BugAI、DoorGate、IceBlock、KeroseneLamp、SafeZone 等）。
启动入口在 `Assets/Scripts/Root/ProjectToolkitBootstrap.cs`、`Assets/Scripts/Root/GameRootApp.cs`。（用它们挂载/初始化音频系统是优先方案，原项目貌似有重复的架构自带音频系统实现，不用管，自己做自己的外部音频系统插入即可）

### 1.2 打点文档
所有你需要的打点信息都在：Assets/Audio Doc

---

## 2. 总体交付物（必须全部完成）

### D1. 运行时集成层（Runtime Integration Layer）
- 一个“音频服务中枢”封装 Audio Manager Pro
- 支持：SFX / Music / UI / Ambient 的基本播放与停止
- 支持：OneShot、Loop、随机、序列、冷却/间隔、音量/音高随机等
- 支持：缺资源安全（留空不报错、只给可选日志）

### D2. 一键环境配置 Editor 脚本（One-Click Setup）
- 菜单按钮：一键把 Audio Manager Pro 需要的对象/Prefab/组件放进场景
- 自动创建/链接：音频配置数据库（ScriptableObject）、必要的 Adapter、可选的 AudioMixer(若插件需要)
- 可重复执行：幂等（重复点击不会生成一堆重复物体）

### D3. 音频绑定与调参编辑器（Audio Binding & Tuning Editor）
- 一个 EditorWindow：按“系统分类 + SFX ID”展示全部音频槽位
- 每个槽位可拖入 0..N 个 AudioClip / Addressable 引用（按项目策略）
- 可设置播放策略与参数（随机/序列/冷却/间隔/音量音高范围/2D-3D/loop/fade/优先级等）
- 一键保存应用：修改立即写入配置数据库，运行时读取生效
- 留空健壮：留空不报错，运行时自动跳过

### D4. 点位插入落地（SFX 插入）
- 对照 `SFX_Dotting_Map.md` 把每个点位插入到对应脚本/函数/状态切换
- 约束：**不得在 Update 每帧无脑 Play**；循环类必须用“进入播放/离开停止”或节流策略
- 插入必须可维护：不要在业务脚本里写一堆硬编码 clip；只触发 `AudioService.Play("SFX-XXX-####", context...)`

---

## 3. 运行时系统设计（强制遵守）

### 3.1 音频事件ID为唯一真相
- 所有音频事件用字符串 ID：例如 `SFX-PLR-0001`、`SFX-INT-0007` 等
- 业务代码只知道 ID，不知道具体 AudioClip

### 3.2 配置数据库（ScriptableObject）
创建一个 ScriptableObject，例如：
- `AudioEventDatabase.asset`
包含：
- List/Dictionary: `eventId -> AudioEventConfig`

`AudioEventConfig` 至少包含：
- `string eventId`
- `AudioCategory category`（SFX/Music/UI/Ambient）
- `List<AudioClip> clips`（允许 0 个）
- `PlayMode`：Single / RandomOne / RandomNoRepeat / Sequence
- `bool loop`
- `float volume` 或 `Vector2 volumeRange`
- `Vector2 pitchRange`（可选）
- `float cooldown`（最短触发间隔，防止连发）
- `float randomIntervalMin/Max`（用于“循环但随机间隔触发”的环境音）
- `SpatialMode`：2D / 3D
- `float spatialBlend` / `minDistance/maxDistance`（如需要）
- `Bus`：SFX / Music / UI / Ambient（用于 mixer group 或插件 routing）
- （可选）`StopPolicy`：StopByEventId / StopByHandle / StopByOwner

> 重点：配置项不是越多越好，但要覆盖你提出的“随机播放、随机间隔、可留空、可调参”。

### 3.3 AudioService（对业务的唯一入口）
实现一个 `AudioService`（MonoBehaviour 单例或纯静态 + 场景中持有者）：
- `Play(eventId, AudioContext ctx = default)`
- `Stop(eventId, AudioContext ctx = default)`（用于 loop）
- `PlayMusic(eventId, ...)` / `StopMusic(...)`（可选）
- `SetBusVolume(bus, value)`（可选）

`AudioContext` 建议包含：
- `Transform owner`（跟随/用于 stop）
- `Vector3 position`（3D 播放）
- `bool isUI`（可映射到 UI bus）
- `float? overrideVolume/pitch`（可选）

AudioService 内部负责：
- 从 `AudioEventDatabase` 取 config
- clips 为空 -> 直接 return（可选 log）
- 根据 PlayMode 选择 clip
- 根据 cooldown / randomInterval 做节流
- 调用 Audio Manager Pro 的实际播放 API

### 3.4 与 Audio Manager Pro 的适配层（Adapter）
不要让全项目直接依赖插件 API。
做一层 `IAudioBackend` 或 `AudioManagerProBackend`：
- 统一 `PlayOneShot(clip, bus, pos, volume, pitch, spatial...)`
- 统一 `PlayLoop(clip, ...) -> handle`
- 统一 `Stop(handle)` 或 `StopByKey(eventId, owner)`（视插件能力）

这样未来换插件不会崩。

---

## 4. 一键环境配置（Editor 工具）要求

### 4.1 菜单入口
提供 `Tools/Audio/Setup Audio (Audio Manager Pro)`：
点击后自动完成：
1) 场景中确保存在 `AudioSystem` 根物体（DontDestroyOnLoad）
2) 挂载 `AudioService` + `AudioManagerProBackend`（或相应组件）
3) 自动创建（或找到）`AudioEventDatabase.asset`，并链接到 AudioService
4) 若插件需要 Prefab/Manager：实例化并放到正确层级
5) 若有示例配置（Mixer/Bus），自动创建或自动绑定
6) 幂等：重复执行只修复引用，不重复生成

### 4.2 校验与提示
执行后输出一个简短报告（Console + 可选弹窗）：
- ✅ 已创建/已存在哪些对象
- ⚠️ 缺少哪些依赖（比如插件脚本改名/asmdef 缺）
- 🔧 建议下一步操作（比如打开绑定编辑器、开始填 clip）

---

## 5. 音频绑定编辑器（EditorWindow）要求

### 5.1 入口与布局
菜单：`Tools/Audio/Audio Binding Panel`

UI 结构（推荐）：
- 左侧：分类树（Player / Enemy / Interactable / Environment / UI / Meta）
- 右侧：该分类下的事件列表（可搜索：按 ID / 中文名关键词）
- 详情面板：选中一个事件后显示配置项与 clips 列表
- 顶部：Database 选择（默认用项目内唯一 asset），并提供“一键修复引用/一键创建”

### 5.2 事件列表来源（必须覆盖全部）
事件清单来源优先级：
1) `SFX_Dotting_Map.md`（最全、带描述）
2) `SFX_Dotting_Index.md`（兜底：至少能列出所有 ID）
3) 若文档缺失：从 `AudioEventDatabase` 自身列出（不推荐但要能用）

> 你需要写一个小型解析器：读取 md 文本，提取形如 `SFX-XXX-####` 的 ID，并按前缀归类。

### 5.3 参数与健壮性
- clips 列表允许为空（表示不绑定）
- 运行时读取到为空：必须安全跳过
- 提供“试听”按钮：在 Editor 下直接播放选中 clip（如果插件支持 Editor 播放；不支持就用 UnityEditor.AudioUtil 或临时 AudioSource）
- 支持批量操作：
  - “为该分类所有事件设置默认 Bus=...”
  - “批量设置 cooldown 默认值”
  - “扫描未绑定事件（空 clips）并导出清单”

### 5.4 数据保存
- 修改后立刻 dirty + 保存 asset
- 支持撤销（Undo.RecordObject）

---

## 6. 点位插入（核心工作流）

### 6.1 插入原则（非常重要）
- **只在“状态变化点/一次性事件点”播放 OneShot**
  - 例：起跳、落地、激活、拾起、触发存档、死亡等
- **循环音必须严格成对：Enter->PlayLoop，Exit->StopLoop**
  - 例：黑暗扣光循环、安全区回光循环、幽灵漂浮循环、门体运动循环、冰块消融循环等
- **严禁 Update 每帧 Play**
- 同一事件若可能短时间多次触发：必须使用 cooldown（配置驱动）

### 6.2 插入方式（统一调用 AudioService）
在对应脚本点位只写：
- `AudioService.Play("SFX-PLR-0001", ctx);`
或循环：
- `AudioService.Play("SFX-ENV-0002", ctx);`（loop 配置驱动）
- `AudioService.Stop("SFX-ENV-0002", ctx);`

### 6.3 插入任务执行顺序（建议）
1) Player（起跳/落地/攀爬/死亡/复活）
2) Interactable（灯/门/藤蔓/冰块/存档点）
3) Environment（黑暗/安全区/危险体积/落石）
4) Enemy（虫子/幽灵）
5) UI（按钮点击）
6) Meta（对话开始/光量耗尽）

> 每完成一个系统：在 Test Scene 做一次“触发回归”。

---

## 7. 验收标准（你必须做到）

### 7.1 功能验收
- 一键 Setup 后：进入游戏，不报 MissingReference/Null
- 打点事件触发：能听到（若已绑定 clip）
- 没绑定 clip：不报错、不播放、可选 debug log
- 循环事件：进入播放、离开停止，不会叠音/不会越播越多
- 冷却有效：高频触发不会音效爆炸

### 7.2 质量验收
- 业务脚本改动尽量小：只插 AudioService 调用
- 插件 API 不泄露到业务层（有 Adapter）
- EditorWindow 可用、不卡、能搜索、能批量
- 数据结构可扩展：未来加新事件只需要在数据库/文档添加，不需要改一堆代码

---

## 8. 你需要输出的“最终文档/报告”（给项目交付用）

完成后你要输出：
1) 《Audio System 集成说明》
   - 一键配置怎么用
   - Audio Binding Panel 怎么用
   - 如何新增一个新 SFX 事件（从文档到代码到绑定）
2) 《点位插入完成清单》
   - 按 SFX ID 列表逐项打勾
   - 标注：对应脚本/函数/触发条件/loop stop 对应点
3) 《未绑定音频清单（可选）》
   - 列出仍为空的事件，方便后续找资源

---

## 9. 重要约束（别踩坑）

- 不要把 AudioClip 引用直接塞到各个业务 MonoBehaviour 上（会维护地狱）
- 不要依赖 Resources.Load（除非项目已有约定），优先 ScriptableObject + Inspector 绑定
- 不要把“随机间隔循环”写死在业务层，应该在 AudioService 里按 config 做
- 插件若有对象池/并发限制，必须尊重：否则会丢音或爆音
- 所有新代码要放在清晰目录，例如：
  - `Assets/Scripts/Independents/Audio/Runtime/*`
  - `Assets/Scripts/Independents/Audio/Editor/*`

---

## 10. （可选）Debug 工具
提供一个全局开关：
- `AudioDebugSettings`（ScriptableObject 或 ProjectSettings）
- 可打开：打印每次 Play/Stop 的 eventId、owner、clip 名称、冷却命中情况
- 默认关闭

---

# 结束
> 按上述顺序完成 D1~D4，并在每个阶段都保证可运行。
